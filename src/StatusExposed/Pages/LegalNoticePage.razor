@page "/legal"

@using BlazorMarkdown
@using Microsoft.Extensions.Options
@using StatusExposed.Models.Options

@inject IOptions<GeneralSettings> GeneralSettings

@if(File.Exists(GeneralSettings.Value.FilePaths.LegalNotice))
{
    <Markdown FilePath="@GeneralSettings.Value.FilePaths.LegalNotice"/>
}